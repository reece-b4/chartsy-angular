import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.chartsyAngular',
  appName: 'chartsy-angular',
  webDir: 'dist/chartsy-angular/browser',
};

export default config;
